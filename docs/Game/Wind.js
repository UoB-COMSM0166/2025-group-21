

class Wind {
    constructor() {
        // game.windSound.setVolume(0);
        // game.windSound.loop();
    }

    adjustVolume() {

        // if (!game.player.alive) {
        //     game.windSound.setVolume(0.0, 0.5);
        // }
        // else if (game.initialDrop) {
        //     game.windSound.setVolume(0.0, 0.0);
        // }
        // else if (game.player.pos.y < height/4 && !game.pause.active) {
        //     game.windSound.setVolume(0.13*settings.masterVolume*settings.mute, 3);
        // }
        // else {
        //     game.windSound.setVolume(0.0, 1.0);
        // }
    }
}